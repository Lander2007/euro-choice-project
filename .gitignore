node_modules/
.next/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*


# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
/.agents/
/.aider-desk/
/.augment/
/.autohand/
/.bob/
/.claude/
/.codeartsdoer/
/.codebuddy/
/.codemaker/
/.codestudio/
/.lingma/
/.openhands/
/.windsurf/

# Duplicated skill installs (identical copies of the agent skills above,
# verified same file hash) - not project source.
/skills/
/data/
skills-lock.json
